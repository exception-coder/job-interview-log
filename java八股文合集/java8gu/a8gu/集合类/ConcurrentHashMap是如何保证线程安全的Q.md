# ✅ConcurrentHashMap是如何保证线程安全的？

# 典型回答

**在JDK 1.7中，ConcurrentHashMap使用了分段锁技术**，即将哈希表分成多个段，每个段拥有一个独立的锁。这样可以在多个线程同时访问哈希表时，只需要锁住需要操作的那个段，而不是整个哈希表，从而提高了并发性能。
​

虽然JDK 1.7的这种方式可以减少锁竞争，但是在高并发场景下，仍然会出现锁竞争，从而导致性能下降。
​

**在JDK 1.8中，ConcurrentHashMap的实现方式进行了改进，使用节点锁的思想，即采用“CAS+Synchronized”的机制来保证线程安全。**
​

**在JDK 1.8中，ConcurrentHashMap会在添加元素时，使用CAS无锁化初始化桶和将元素插入空桶；针对已有节点的桶，使用synchronized加锁，再次尝试put。这样可以避免分段锁机制下的锁粒度太大，以及在高并发场景下，由于线程数量过多导致的锁竞争问题，提高了并发性能。**
​

# 扩展知识

## 源码分析

ConcurrentHashMap将哈希表分成多个段，每个段拥有一个独立的锁，这样可以在多个线程同时访问哈希表时，只需要锁住需要操作的那个段，而不是整个哈希表，从而提高了并发性能。下面是ConcurrentHashMap中分段锁的代码实现：
​

在上面的代码中，我们可以看到，每个Segment都是ReentrantLock的实现，每个Segment包含一个HashEntry数组，每个HashEntry则包含一个key-value键值对。
​

接下来再看下在JDK 1.8中，下面是ConcurrentHashMap中CAS+Synchronized机制的代码实现：
​

1.8 的 CHM 使用 `synchronized` 来锁定单个桶的头节点，而不是整个数组或一个段（Segment）。
​

当要插入的桶是空的时候，使用 CAS (`**casTabAt**`) 来尝试将新节点放入该桶，而不是直接加锁。如果 CAS 失败，说明发生了竞争，则转而使用加锁的策略。
​

当要操作的桶不为空时（可能是链表或红黑树），会对该桶的头节点进行 `**synchronized**` 加锁。这样，一个线程在操作链表 a 时，另一个线程仍然可以同时操作链表 b，并发度大大提升。
